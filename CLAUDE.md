# CLAUDE.md — מדריך המנהל של פרויקט תיק ההשקעות

> קרא קובץ זה תחילה, בכל סשן, לפני שנוגעים בכל דבר.
> קובץ זה הוא המוח המשותף של שני המנהלים (Muse ו־Claude). כשאתה לומד לקח עמיד — עדכן אותו.

## 1. מה הפרויקט

אפליקציית PWA בעברית ("תיק ההשקעות") לניהול ומעקב תיק השקעות, עבור המשתמש.
נבנתה 2026-09-22, מתוחזקת מאז ברציפות.

- ריפו (ציבורי): https://github.com/yishaiguedj1/portfolio-pwa
- אתר חי: https://yishaiguedj1.github.io/portfolio-pwa/
- הפריסה: GitHub Pages (קבצים סטטיים, `.nojekyll` בשורש)

## 2. מפת ארכיטקטורה

| קובץ/תיקייה | תפקיד |
|---|---|
| `app.js` | כמעט כל הלוגיקה (כ־270KB). גרסה: `APP_VERSION` |
| `index.html` | מבנה + מסכים |
| `styles.css` | עיצוב (בהיר/כהה) |
| `sw.js` | Service Worker. גרסת קאש: `CACHE_NAME` |
| `returns.js`, `cloud.js`, `firebase-config.js` | מנוע תשואות (מיזוג, TWR, רווח, XIRR — בלי פארסר CSV מאז v128), ענן, Firebase |
| `manifest.webmanifest`, `icon-*.png`, `logo-header.png`, `apple-touch-icon.png`, `favicon-48.png` | PWA + מיתוג "THE SNOWBALL" (v129→v131): לוגו שקוף בכותרת (106px, נייד 80px) במקום הטקסט "תיק ההשקעות" (נשאר ל־sr-only/‏title). מבנה v131: הלוגו ראשון ב־DOM בתוך `.appbar-row` (מציג בצד ה"התחלה" — ימין ב־RTL); כפתורי הפעולה + שורת "מקור" מקובצים יחד ב־`.appbar-left` בצד השני (שמאל), טור אנכי, ממורכז מול גובה הלוגו (`align-items:center` בשורה). אייקוני הכפתורים ירוקים (`var(--primary)`) — זה המקור, לא לשנות לכחול. icon-192/512 שקופים (any), `icon-maskable-512.png` ו־`apple-touch-icon.png` על רקע כהה `#101613`. המקור הגיע עם משבצות "שקיפות" צרובות בפיקסלים — הוסרו במילוי מהקצוות. לקח: שינוי עיצוב שמשתמש מבקש — תצוגה מקדימה (mockup סטטי, headless) לפני נגיעה בקוד האמיתי, במיוחד כשגדלים/מיקומים משתנים בכמה סבבים. **v140**: `.appbar-left` (טור: כפתורי פעולה + שורת "מקור") היה `align-items:flex-start` — בקונטיינר RTL זה מצמיד לקצה שגדל/מתכווץ לפי אורך שורת המקור (רגיל מול "· אחרי־מסחר"), לא לקצה המסך. כשהטקסט התארך, כפתורי הפעולה (צמודים לאותו קצה) זזו לכיוון הלוגו. תוקן ל־`flex-end` — מצמיד לקצה שבאמת עוגן למסך (flush ב־`justify-content:space-between`). לקח כללי: בטור RTL עם ילדים ברוחב משתנה, לבדוק תמיד לאיזה קצה `align-items` באמת מצמיד — הקצה "היציב" ויזואלית הוא זה שעוגן לקונטיינר החיצוני, לא בהכרח `flex-start`. |
| `ibkr-proxy/` | שרתון Vercel ל־IBKR Flex — **מתפרס אוטומטית מ־GitHub בכל push**. נקודות: `/api/flex-request` (SendRequest), `/api/flex-statement` (GetStatement) |
| `tests/` | בדיקות node. הרצה: `node tests/<file>` או `bash tests/run-all.sh` |
| `ibkr-proxy/tests/run.js` | בדיקות השרתון (88, כולל שמירת גישה) |
| `יומן-עבודה.md` | יומן עבודה בעברית — מתעדכן בכל גרסה |
| `תהליך-עבודה.md` | תהליך העבודה צעד־אחר־צעד: מבקשה ועד אישור בטלפון (עם דוגמת v121) |
| `design-mockups/` | מוקאפים עיצוביים |
| `HANDOFF_FOR_CLAUDE.md` | מסמך היסטורי (handoff של v81) — לא קנוני |

## 3. פרטיות נתונים — קריטי, אין יוצאים מן הכלל

- **גם ביומן ובלקחים: מספרים מוסווים בלבד** — לא תשואה מדויקת של התיק, לא כמות, לא מחיר קנייה, לא תאריך קנייה, לא מספר חשבון, לא שם המשתמש. ‏`tests/privacy-guard.test.js` אוכף חלק מזה ב־CI.
- **נתונים אמיתיים חיים רק בענן של המשתמש** (Firestore, התחברות Google, per-user `users/{uid}`). הריפו הציבורי מכיל תיק דמו פיקטיבי בלבד.
- **v146 — אין נתוני ברירת מחדל**: `DEFAULT_DB` ריק (תיק חדש/אחרי איפוס/משתמש חדש בענן). עד v145 הוא הכיל נתוני דוגמה שנשמרו כנתונים אמיתיים — `stripLegacyDemo` מנקה אותם בטעינה ובענן. לא להחזיר נתונים לברירת המחדל.
- **שני מאגרים (v146)**: ידני מול IBKR. הפקדת IBKR = `src:'ibkr'` או תאריך ISO (`isIbkrDeposit`; ידני = DD/MM/YYYY); מניית IBKR = במצב IBKR כל מה שלא `src:'manual'`. איפוס נפרד: `resetManualData` / `resetIbkrData` + איפוס מלא.
- **תגית מקור (v147)**: `srcTagHTML('manual'|'ibkr')` על כל פריט (מניות, עסקאות, הפקדות, פנסיה, כפתורי איפוס). IBKR = `ibkr-logo.png` (ב־APP_SHELL של sw.js). המקור נגזר מ־`positionSource`/`isIbkrDeposit` — אותן פונקציות של האיפוס; לא להמציא כלל סימון נפרד.
- **תיק דמו (v146→v161)**: `demoCreate` — נבנה בזמן אמת ממחירי Yahoo (לא נתונים קבועים בקוד). **מ־v161: 6 שנים** לפי `DEMO_PLAN` (קניות מדורגות, SPY/QQQ כל חצי שנה, EIS, 11 מניות ארה"ב, 8 ישראליות, מימושים בשיא) + 2 "לוהטות" (`demoEntry` ברווח יעד) + `DEMO_FUNDS` (2 פנסיה + 2 השתלמות, הפקדה לכל שנה). היסטוריה בדמו: 10y מ־Yahoo, נשמרת רזה `histSlimForDemo` (7 שנים, תאריך+סגירה) — המבנה המלא הגיע ל־5.6MB בזיכרון הדפדפן. נבדק על נתונים אמיתיים: כל טווחי התשואה חיוביים. **v162**: לא נכשל כש־Yahoo מגביל — משיכה 4 במקביל + ניסיון חוזר + מטמון, היסטוריה קצרה = קנייה ביום הראשון שיש, מינימום 4 מניות (Yahoo חסום → רק ארה"ב מ־Stooq). מסך טעינה `demoProgressOpen` (שלבים + פס). `ensureFxHist(earliest)` משלים אחורה מטמון קצר. `DB.demo`: לא נכתב לענן (`demoOn` ב־cloud.js) ולא נדרס ממנו; גיבוי `pwa_predemo_v1`; IBKR חסום; `demoExit` משחזר ומנקה היסטוריה.
- **אסור** להכניס ל־git / צ'אט / לוגים / פיקסטורות / צילומי מסך: טוקני Flex, Query ID, מספרי חשבון, אחזקות, יתרות, עסקאות, שמות קבצים, ביצועים, XML/CSV.
- מפתחות API (Twelve Data) נשמרים ב־localStorage של הטלפון בלבד (`pwa_tdkey_v1`). **מ־v148 גם לא מועתקים לענן** (`cloud.js` מוחק עותק ישן: `FieldValue.delete()`).
- **אבטחת השרתון (v148)**: `guard` ב־`ibkr-proxy/lib/ibkr.js` — Origin מאושר בלבד (`ALLOWED_ORIGINS` + localhost), POST בלבד, `X-App-Key` מול `process.env.APP_KEY` (כשמוגדר), גוף ≤4KB, token ≤64 / queryId ≤12 / code ≤32. באפליקציה: `ibkrProxyHeaders()` בכל קריאה; **שדה "מפתח שרתון" הוסר ב־v150** (רדום — להפעלה מחדש להחזיר שדה שכותב ל־`ibkrCfg().appKey`). אם האתר עובר לדומיין אחר — להוסיף אותו ל־`ALLOWED_ORIGINS`, אחרת הסנכרון נחסם. **מצב 25/09/2026: `APP_KEY` לא הוגדר ב־Vercel** (המשתמש לא הצליח בממשק של Vercel, והוחלט לדלג) — השרתון מוגן ב־Origin בלבד, וזה מספיק לשימוש אישי. לא לנדנד; אם המשתמש ירצה בעתיד — להדריך עם צילומי מסך.
- **CSP (v149)**: `<meta http-equiv="Content-Security-Policy">` ב־`index.html`. מקור רשת חדש (API/תמונות/סקריפט) — **חובה** להוסיף למדיניות, אחרת הדפדפן חוסם בשקט. אסור `onload=`/`onerror=`/`onclick=` בתוך HTML (משתמשים ב־`addEventListener`; לתמונות — `data-logo`/`data-err` + `wireImgEvents`). שינוי בסקריפט ה־inline של ערכת הנושא — לעדכן את ה־hash (`tests/csp.test.js` יכשל ויגיד). שדרוג Firebase — לעדכן `integrity`. אם התחברות Google נשברת אחרי שינוי מדיניות — לבדוק `frame-src`/`script-src` (apis.google.com, firebaseapp.com).
- **שומרים אוטומטיים ב־CI**: `tests/privacy-guard.test.js` (מספר חשבון, טוקן, מפתח Google, מייל — בכל קובץ בריפו), `tests/xss-guard.test.js` (שדה חיצוני ב־`innerHTML` בלי `esc()`). אם נכשלים — לתקן את המקור, לא להחליש את הבדיקה.
- **CI**: `permissions: contents: read`, actions מקובעים ל־SHA. ניתוק IBKR מוחק token/Query ID/מפתח שרתון מהטלפון.

## 4. IBKR Flex — הידע שנצבר בדם

- נקודת Flex הנכונה: `/AccountManagement/FlexWebService` — הנתיב הישן `Universal/servlet/...` מחזיר תמיד 1001.
- User-Agent דינמי של Node; שני הוסטים עם fallback: `ndcdyn` / `gdcdyn` — החשבון של המשתמש על **gdcdyn (אירופה)**.
- מגבלה רשמית: **בקשה לשנייה, עד 10 בדקה, לטוקן** (לא ל־IP). גם SendRequest וגם GetStatement נספרות.
- קצב (v121): **מגביל מתגלגל משותף** `IBKR_LIMITER` (`ibkrMakeLimiter`) — כל בקשה ל־IBKR (SendRequest + GetStatement, כולל "בדוק חיבור") עוברת דרכו: עד 8 בחלון 60 שניות, ≥1.5 שניות בין בקשות. אין הפוגה קבועה בין חלקים (הפוגת 60 השניות של v120 הפכה 10 שנים ל־~13 דקות; עכשיו ~4 דקות בסימולציה). שאילתה ראשונה אחרי `IBKR_POLL_FIRST_MS = 4000`, אחר כך כל `IBKR_POLL_DELAY_MS = 6000`, `IBKR_POLL_TRIES = 30` (~3 דקות).
- **כשל שרתון ≠ "עדיין מייצר"** (v122): תשובה לא־JSON (timeout של Vercel) / `ibkr_http_*` / `fetch_failed` בזמן ההמתנה — עד `IBKR_POLL_MAX_FAILS = 3` רצופים ואז שגיאה עם הקוד האמיתי (`proxy_http_504` וכו'). לפני v122 זה נבלע בשקט — חלק אחד יכול היה "להיתקע" 30+ דקות.
- **אחרי שיש reference code לא מזמינים דוח חדש** (רק 1003 מצדיק) — SendRequest חוזר מכפיל זמן ומסכן 1025.
- השרתון: `ibkrGetMulti` עם תקציב זמן 24 שניות לכל ההוסטים — מחזיר JSON לפני ש־Vercel (maxDuration 30) הורג אותו.
- UI: שורת מצב חיה (חלק · שלב · זמן, `ibkrStatusLine`) + Wake Lock למסך במהלך המשיכה (טלפון שנכבה משהה את הדף).
- **v123 — פער שקט בין חלקים**: חלק שנכשל בהמתנה (לא throttle/1025/1003) ואינו אחד משני הכשלונות הרצופים שעוצרים — נשמט בשקט מהתוצאה, ו־`meta.fromDate` היה ננעל על החלק הראשון שהצליח (לא בהכרח הכי ישן, אם סדר העיבוד לא כרונולוגי). תוקן: סבב ניסיון נוסף אחד בסוף `ibkrFetchFullHistory` לכל חלק כזה (לא ל־1003 — הוא כבר קיבל את מכסת `ibkrChunkRetryPlan`; לא ל־throttle — אסור לנסות שוב); `meta.fromDate` = המינימום מכל חלק שהתקבל בפועל, לא תלוי בסדר. חלק שעדיין נכשל אחרי הסבב הנוסף נשאר מתויג ב־`_chunks`, ומזהיר בדיאלוג האישור (`ibkrGapWarn`) — כולל תאריך ההתחלה המדויק שצריך להזין ידנית כדי להשלים את הפער (סנכרון רגיל ממשיך קדימה בלבד, לא אחורה).
- **1018** = הגבלה רגעית ("נסה שוב בעוד רגע"). אין "קופסת עונשין של 10 דקות" בתיעוד Flex (זה מ־API המסחר — בלבול נפוץ שתוקן ב־v120). **אין ניסיון חוזר על 1018** — לא בשרתון (הוסר מ־`TRANSIENT_FLEX_CODES`) ולא באפליקציה.
- **1025** = נעילת טוקן לא־מתועדת (~14 שעות), נגרמת מניסיונות SendRequest חוזרים. האפליקציה עוצרת מיד (`_locked`) עם הודעות `importLocked` / `ibkrErrLocked`: להמתין שעות, לא לנסות מוקדם.
- **1003** = אין נתונים לטווח — מדלגים בחן. **v137**: 1003 על חלקים ישנים *לפני* שחלק כלשהו החזיר נתונים = שנים שלפני פתיחת החשבון (משיכת 5 שנים לחשבון בן ~3) → `beforeStart`→`noData`, לא נספר ב"שני כשלונות רצופים" ולא מזהיר על פער. רק החלק העדכני מקבל ניסיונות חוזרים על 1003 (דוח שעוד לא פורסם); חלק ישן = בקשה אחת. 1003 *אחרי* נתונים (חור באמצע) או כשכל החלקים נכשלו — עדיין כשל גלוי.
- מקסימום 365 יום לבקשה; `ibkrDateChunks` חותך מהעבר קדימה (oldest-first) **לפי שנים קלנדריות** (v124): כל חלק מלבד הראשון מתחיל ב־1/1; שנה מעוברת מלאה = 1/1–30/12 + 31/12.
- **IBKR מסרב (flex_1003) לטווח ישן שמתחיל באמצע השנה** — הוכח 24/09/2026: 23/09/2023–21/09/2024 נכשל, 01/01/2023–31/12/2023 עבד. לכן `ibkrDepthStartYmd` מחזיר 1 בינואר של שנת ההתחלה (3 שנים = מ־1/1/2023). טווחים מהשנתיים האחרונות עובדים גם מאמצע השנה. 1003 כאן ≠ "אין נתונים".
- עומק היסטוריה: בורר משתמש 1/2/3/5/10 שנים (ברירת מחדל 5, נשמר ב־`ibkrHistoryDepth`), `ibkrDepthStartYmd(years)` מחשב תאריך התחלה; תאריך מדויק עוקף וללא הגבלה (גם 10+ שנים). כשיש נתונים — סנכרון אינקרמנטלי מהסוף הקיים.
- **סנכרון המשך (v127)**: מתחיל ביום **שאחרי** `meta.toDate` (`ibkrDefaultFromYmd`), לא ביום עצמו — תקופה שמתחילה ביום שבו הקודמת נגמרה "נוגעת" בלבד ב־`rPeriodsOverlap`, שתיהן נשמרות, והיום נספר פעמיים ברווח וב־TWR. לא לשנות את כלל החפיפה במקום זה: תקופה קצרה חופפת הייתה מחליפה שנה שלמה. ערך ההצעה בשדה התאריך (או תאריך הסיום הישן) = המשך, לא נשמר כתאריך ידני. אם נותרו רק ימי סופ"ש/כבר מעודכן (`ibkrHasWeekday`) → `ibkrUpToDate`, בלי בקשה ל־IBKR. `rMergePeriods`: תקופה קצרה שכולה בתוך קיימת רחבה — מדלגים (לא מוחקת אותה); טווח זהה עם ערכים חדשים — עדיין מחליף.
- **תאריך הסיום = יום המסחר האחרון שנסגר בניו־יורק (v136)**, `ibkrLastClosedDate()` (Intl, `America/New_York`) — לא "אתמול" לפי שעון ישראל. בחצות בישראל עדיין 17:00 בניו־יורק: "אתמול" הישראלי = היום האמריקאי שעוד לא נגמר → flex_1003 על החלק העדכני וכל הסנכרון נכשל (קרה 25/09/2026 00:17). אם החלק העדכני עדיין מקבל 1003 (הדוח מתפרסם בבוקר בארה"ב) — ניסיון גיבוי **אחד** שמסתיים ביום החול הקודם (`ibkrPrevWeekdayYmd`, `no1003Retry`), סנכרון ההמשך הבא משלים. בבדיקות: "אתמול" דרך `ibkrLastClosedDate` של האפליקציה — אחרת CI נכשל בין 00:00–04:00 UTC.
- **השוואה ל־PortfolioAnalyst**: נתוני Flex נגמרים בסגירת אתמול; PortfolioAnalyst עם Real-Time עד הרגע. להשוואה: CUSTOM עד אתמול, Real-Time Off. רווח = Change in NAV − Deposits & Withdrawals.
- `<Transfer>` נקרא כעסקת מזומן (`Transfer IN/OUT`) ונחשב כהפקדה. פוזיציות: רק `SUMMARY` (Flex מכפיל כל פוזיציה: SUMMARY + LOT).
- אל תפציץ את IBKR בבדיקות — רצפי בדיקות מהירים מכניסים IP לקופסת עונשין. בדיקות = mocks בלבד, לעולם לא הטוקן האמיתי.

## 5. מנוע תשואות ונתוני שוק

- TWR: שחזור לאחור מעסקאות (`buildTradesHistory`) — מבטל קניות/מכירות/עמלות/תזרימים; דיבידנדים נחשבים כתשואה; המרות מט"ח מוחרגות; אופציות מסוננות (רק BRK B); שורטים כפוזיציות שליליות; רשת ביטחון לספליטים ידועים (`KNOWN_SPLITS`: NOW 5:1 ב־2025-12-18).
- מוצג **רק TWR רשמי של IBKR** כשזמין (ChangeInNAV) — לעולם לא מנחשים TWR רשמי חסר.
- **אחזקות ידניות (v141)**: פוזיציה ידנית = `src:'manual'` — **נשמרת בסנכרון IBKR** (`ibkrFinishImport` מחליף רק את פוזיציות IBKR; לפני v141 כל סנכרון מחק מניות שנוספו ביד). הוספה ביד במצב IBKR לפני v141 מסומנת אוטומטית (`markManualPositions` ב־`renderAll`: פוזיציה שלא בדוח). שני מצבים: **לפי מחיר ממוצע** (כמות+ממוצע; בשווי וברווח, לא בתשואה לאורך זמן) ו**לפי עסקאות** (`DB.manualTrades` — `{id,date,sym,side,qty,price,fee}`, `fromTrades:true`; כמות/ממוצע נגזרים ב־`mtPosition` בשיטת עלות ממוצעת + רווח ממומש; `mtSyncPositions` אחרי כל שינוי). ולידציה `mtValidate`/`mtOversold`: לא תאריך עתידי, לא למכור יותר ממה שמוחזק בשום נקודה בזמן (כולל עריכה/מחיקה). מניה ש־IBKR מחזיק לא ניתנת להזנה ידנית; עסקאות ידניות שלה מוצללות (`mtShadowed`, לא נספרות). סקירה במצב IBKR: שווי = דוח + שווי ידני חי; רווח = `rGain` + `manualTotalsUSD`; תשואה = **TWR משולב** (`ibkrReturnRows`/`rowsWithManualTwr`): הסדרה הרשמית עד היום שלפני העסקה הידנית הראשונה, מאז r=(NAV+M−F−G)/(NAV₋₁+M₋₁)−1 — קנייה/מכירה ידנית = תזרים (לא רווח). דורש NAV יומי ובסיס USD; אחרת `needsDaily` ונשאר רשמי. `ovMeta` אומר איזה סוג. ענן: `cloud.js` מעתיק `manualTrades`. במצב ידני (בלי IBKR) — הסקירה לא שונתה (שווי מול הפקדות).
- **מצב ידני = אותה סקירה כמו IBKR (v158)**: `manualTwrRows` — TWR בדולרים מהעסקאות הידניות; יום עם עסקה = שתי תת־תקופות (המניה שנסחרה לפי מחיר העסקה, **השאר לפי הסגירה של אותו יום** — לפי הסגירה הקודמת התנועה שלהן נמהלת בכסף החדש, טעות של ~4 נקודות בדמו). אומת מול נוסחת סוף־יום עצמאית (עסקאות בסגירה → זהה עד עמלות). בלי עסקאות עם תאריך — `holdingsUsdRows` (אחזקות נוכחיות בדולרים) מול המדדים. `pfShowBench`: `ibkr`/`trades`/`holdings`. כותרת = `paintManualHead`, כרטיס = `renderManualPerf` באותו `#ibkrPerfCard` (כותרת `#ibkrPerfTitleEl` מתחלפת). מניות "לפי ממוצע" — ברווח, לא ב־TWR/XIRR (כמו ב־IBKR).
- **גרף הביצועים (v125)**: טווחים לפי **תאריך** (`pfRangeCutoff`/`pfSliceRange`), לא לפי מספר שורות — `filterRange` ישן הניח שורה ליום מסחר, ובנתוני IBKR (שורה לשנה) כל טווח לקח הכל. אם אין נקודה עד 10 ימים מתחילת הטווח → לא מציגים תשואה (`pfRangeNeedsDaily`), לא ממציאים. YTD מדויק גם בנתוני שנים (בסיס 31/12).
- **NAV יומי**: מקטע Flex "Net Asset Value (NAV) in Base" → `EquitySummaryByReportDateInBase` → `navDaily`. `rDailyTwrSeries`: r=(NAV_t−F_t)/NAV_{t−1}−1 עם תזרימים מ־`ibkrFlowsByDate`, **מעוגן** לכל תקופה רשמית (מכפלת הימים = TWR רשמי) רק כשהכיסוי מלא. `rCombinedTwrSeries` = תקופות לפני + יומי אחרי. ציר X לפי זמן (`pfTimeFractions`).
- **גרף המניה (v134)**: טווח לפי תאריך (`stockRangeRows` — בסיס = סגירה אחרונה ≤ תאריך החיתוך; שבוע = 7 ימים; **YTD = סגירת יום המסחר הראשון של השנה, כמו Google** — v135, אומת: NOW 147.45 ב־02/01/2026 → −6.56% זהה ל־Google. v134 השתמש ב־31/12 בטעות), סיום במחיר החי (`stockChartRows`, לפי `q.mdate` = תאריך הבורסה, לא בטרום־מסחר). `filterRange` (ספירת שורות) כבר לא בשימוש באפליקציה — נשאר רק לבדיקות ישנות. הגדרת התשואה = כמו Yahoo/Google; אפליקציות מסחר אחרות משתמשות לפעמים בבסיס אחר (NOW: 190.78 שלא קיים כסגירה) — לא לרדוף אחרי המספר שלהן. נבדק 24/09/2026: אפליקציית המסחר של המשתמש מחשבת מ**מחיר הפתיחה של הנר השבועי הראשון** בגרף — YTD מ־146.51 = פתיחת 05/01/2026 (מפספס את 02/01, ‏153.19→147.45, ‏−3.7%), שנה מ־190.78 = פתיחת 22/09/2025. מקור ההשוואה הנכון = Google (המשתמש משווה אליו). לקח: לא להכריז "כמו Google" בלי לבדוק את המספר של Google בפועל. גרף התיק (TWR) נשאר עם בסיס 31/12 — שם זה תואם IBKR.
- **לחיצה על גרף הביצועים (v126)**: לחיצה רגילה = טולטיפ בלבד (`state.pfTipIdx`); מדידה בין שתי נקודות רק אחרי כפתור "מדידה" (`state.pfMeasure.on`) — אותו `chip-btn` כמו בגרף המניה. הסבר מצב המדידה ב־`flash` (שורה קבועה הזיזה את הגרף מתחת לאצבע).
- **רווח/הפסד**: `netFlows` בתקופות Flex מגיע מ־ChangeInNAV (depositsWithdrawals+assetTransfers+internalCashTransfers+debitCardActivity+billPay). לפני v125 היה 0 → כל הפקדה נספרה כרווח.
- מדידה בדולרים בלבד, בלי התאמת שקל (הוראת המשתמש).
- גרפי היסטוריה: Yahoo query1/query2 + Stooq במרוץ (`Promise.any` — הראשון שעונה מנצח), Twelve Data כגיבוי אחרון (throttle של 8 שניות, קאש תוך־יומי 10 דקות).
- ציטוטים חיים: Yahoo ראשי / CNBC גיבוי; טרום־מסחר: Yahoo extended-hours + שדה CNBC (`טרום־מסחר`).
- **מחירים חיים (v139)**: Yahoo `chart` (interval=1m) הוא **זמן אמת** — נמדד 24/09/2026 אחרי־המסחר: הנר האחרון בן שניות (התווית הישנה "דיליי ~15 דקות" הייתה טקסט קבוע, לא מדידה). לולאה `liveTick`: כרטיסים פתוחים כל 5 שניות, כל התיק כל 10 (`LIVE_ALL_EVERY`), ~50 בקשות בדקה בזמן מסחר; מאט ל־60 שניות אחרי 6 טיקים מלאים בלי תזוזה (שוק סגור). עוצר: רקע (`document.hidden`), מצב עריכה, שדה בפוקוס, סנכרון IBKR (`state.ibkrSyncing` מ־`ibkrSetBusy`). **עדכון במקום** (`renderLive`) — אסור לקרוא ל־`renderAll`/`renderStocks` בטיק: מאפס טפסים, מיון, כרטיס פתוח ומדידה. `renderOverview(true)` = בלי גרף הביצועים/דוחות/IBKR. גיבוי: CNBC בבקשה אחת (מסומן "דיליי"). התווית "חי" רק כשהמקור Yahoo. גרף יומי: `intradayLiveRows` + `q.mtime`. בלי שרתון ובלי מפתח — אלפקה/WebSocket לא נחוצים לזה. batch של Yahoo (`/v8/finance/spark`) לא אומת ל־CORS — לא בשימוש.
- **Yahoo חוסם (429) → הפסקה, לא הפצצה (v160)**: 429 מגיע בלי CORS ונראה כחסימה. `yahooGate` (דקה → 10 דקות) בלולאה החיה, CNBC בינתיים; `yahooRecovered` משלים היסטוריות וגרפים פתוחים. Yahoo = המקור היחיד להיסטוריית ת"א (Stooq ארה"ב בלבד) — לכן כשהוא חסום רק הישראליות בלי גרף. שינוי יומי בלי היסטוריה מ־`q.prev`. לא לקצר את ההפסקה ולא להחזיר ניסיון כל 5 שניות.
- **מניות ת"א (v142)**: סימבול Yahoo עם `.TA` → `symCur` = ILS. **Yahoo מדווח ת"א באגורות (`currency: 'ILA'`)** — `parseYahooQuote`/`parseYahooBars` מחלקים ב־100. מחיר/ממוצע/עסקאות נשמרים במטבע המניה (₪); **הזנה ביד — באגורות (v157)**, כמו הציטוט הרשמי: `pxFromInput`/`pxToInput` בכל שדה מחיר (עמלה בשקלים), `pxHints` מציג "= ₪…"; `fixAgorotEntries` מתקן פעם אחת ערכים ישנים שהוזנו באגורות (יחס 30–300 למחיר החי) — לא להוריד בלי מיגרציה אחרת; כל סכום (שווי, רווח, משקל, עוגה, עלות, `manualTotalsUSD`, TWR משולב לפי שער של כל יום) עובר ב־`nativeToUSD`. תצוגת מחיר: `fmtPx` (ת"א תמיד ₪). בחיפוש מוצעים רק ארה"ב ו־.TA (`searchMarketOk`) — בורסות אחרות במטבעות לא נתמכים. Twelve Data לא ל־.TA. **CNBC כן מחזיר .TA — באגורות בלי סימון (v159)**: `parseCNBCQuotes` מחלק ב־100, ו־`taseFixQuotes` (ציטוט פי 30–300 מהסגירה/הממוצע) בכל מיזוג ציטוטים ובשחזור מטמון. **תצוגת מחיר ת"א = אגורות** (`fmtAg`, "7,830 אג׳" בבידוד RTL; בקשת המשתמש) — סכומים נשארים ב־$/₪. **לוגו ת"א = TradingView** (`TASE_LOGOS` + `logoSrc`; FMP החזיר תמונות אקראיות), בלי היפוך צבעים; מניה חדשה לרשימה — להוסיף מזהה (symbol_search של TradingView, שדה logoid). IBKR ממפה רק פוזיציות USD (מניות ת"א ב־IBKR — לא נתמך עדיין).
- **חיפוש מניות (v142)**: מקומי מיידי בכל הקשה (`POPULAR_STOCKS` + `TASE_STOCKS` עם שמות בעברית — Yahoo מחזיר 400 על עברית); רשת במקביל (search API + בדיקת סימבול) עם `SEARCH_TIMEOUT_MS` 3500 לכל מקור, תוצאות מתעדכנות תוך כדי, מטמון לפי שאילתה, השהיה 150ms. Yahoo search עצמו עונה ב־0.2–0.4 שניות; האיטיות הייתה הרצף בלי timeouts.
- **TWR משולב — לקח (v142)**: חלק IBKR לפי הסדרה המעוגנת לרשמי (`baseRows`), לא NAV גולמי — בלי ידניות זה בדיוק הרשמי. המשתמש שאל למה התשואה ירדה אחרי שהוסיף מניה ידנית שבכרטיס שלה רווח יפה: זה נכון — ה־YTD של המניה היה נמוך מזה של התיק, והרווח בכרטיס הוא מהמחיר הממוצע, לא מתחילת השנה. TWR משולב = ממוצע משוקלל של התשואות באותה תקופה; מניה שעשתה פחות מהתיק מורידה אותו. אם מזינים עסקה אחת "לפי ממוצע" בתאריך אחד — מחיר שונה מהסגירה באותו יום יוצר קפיצה מלאכותית; להזין תאריך ומחיר אמיתיים לכל קנייה.
- **כותרת TWR משולב (v144)**: לא `last/first` של הסדרה — `combinedHeadlineTwr` = רשמי × (משולב.last / בסיס.last). אצל המשתמש ה־NAV היומי מתחיל 09/2023 והתקופות הרשמיות 01/2023, ואין תקופה שמתחילה ליד תחילת היומי → `rCombinedTwrSeries` מחזיר רק את היומי; `last/first` מדד מ־09/2023 ויצא בדיוק כמו "3 שנים" (ולא כמו הרשמי מתחילת החשבון). לקח: כל מספר "משולב" חייב להיות על אותה תקופה כמו המספר שהוא מחליף. נבדק: NAV יומי חלקי + תיקון = תוצאת NAV יומי מלא. זהויות מתמטיות ב־`tests/pf-v143.test.js` (קנה־והחזק = יחס שווי, הפקדה לא נספרת, קנייה בסגירה בלי קפיצה).
- **הפקדות מול קניות ידניות (v145)**: TWR לא צריך רשומת הפקדה לקנייה ידנית — היא כבר תזרים בנוסחה המשולבת. אבל טאב ההפקדות במצב IBKR כולל אותן (`manualFlowsILS`: קנייה=הפקדה, מכירה=משיכה, שער יום העסקה) כדי ש"סך הפקדות" יתאים לשווי הכולל. `netDepositsILS` נשאר IBKR/ידני בלבד. לקח: תרומת מניה ל־YTD = משקל ביום הקנייה × העודף שלה מעל התיק **מאותו יום** — לא התשואה שלה מהממוצע.
- מט"ח: Yahoo `USDILS=X` תוך־יומי, גיבוי er-api/frankfurter.
- יעדי אימות: התשואה מתחילת החשבון ו־YTD חייבות להתאים למספרים הרשמיים שהמשתמש רואה ב־IBKR (הערכים עצמם לא נרשמים כאן — פרטיות).

## 6. מחזור עבודה

1. קרא קובץ זה + זנב `יומן-עבודה.md` + בדוק גרסה חיה מול מקומית.
2. ערוך קבצים. 3. הרץ בדיקות רלוונטיות (סעיף 7). 4. פרוס לפי פרוטוקול סעיף 8.
5. עדכן `יומן-עבודה.md` (עברית, כל גרסה) וקובץ זה אם למדת לקח עמיד.

## 7. בדיקות

- `node --check app.js` תמיד לפני push.
- `node tests/ibkr-app.test.js` — לוגיקת IBKR/קצב/עומק/הודעות (205 אסרטים, כולל שמירה שה־CSV לא חוזר). `node tests/pf-v125.test.js` (41) — גרף/טווחים/NAV יומי. `pf-v126.test.js` (10) — טולטיפ מול מדידה. `pf-v127.test.js` (18) — יום כפול בסנכרון המשך.
- `node tests/pf-v110.test.js` (28), `pf-v111.test.js` (19) — רגרסיות סנכרון; `pf-v112.test.js` (61) — מנוע התשואות על נתוני דוח סינתטיים.
- `node tests/i18n-coverage.test.js` (711) — כל מחרוזת גלויה בעברית **ובאנגלית**.
- `node ibkr-proxy/tests/run.js` (62) — שרתון.
- הכל: הלולאה ב־`.github/workflows/test.yml` (אין `tests/run-all.sh` בריפו). בדיקות תאימות `CACHE_NAME`↔`APP_VERSION` אדומות בין שלב התוכן לשלב ה־sw.js — צפוי, מתייבש אחרי דחיפת sw.js.
- מוסכמה: בבדיקות multi-chunk מעבירים `{ chunkGapMs: 5 }` — זה מחליף את כל הקצב (בלי מגביל, בלי המתנת poll ראשונה). לבדיקת קצב אמיתי: `{ limiter: ibkrMakeLimiter({now, sleep}), sleep }` עם שעון מדומה.
- 13 קבצי בדיקות מורשתיים מוחרגים מ־CI (רשימה ב־`.github/workflows/test.yml`): בודקים פונקציות/אלמנטים שהוסרו בריפקטורים (`buildTradesHistory`, `navToTwr`, `ibkrInceptionDate`, `ibkrNavHistory`, `ibkrPerfSums`, `ibkrChunks`). נשמרים כהיסטוריה; החייאה = כתיבה מחדש.

## 8. פרוטוקול פריסה — קריטי, לקרוא פעמיים

**לעולם לא דוחפים את `sw.js` יחד עם קבצי תוכן.** טלפון שבודק עדכונים באמצע ה־push עלול להתקין SW חדש כשה־CDN עוד מגיש app.js/index.html ישנים — ומרעיל את הקאש החדש לצמיתות ("תקוע על גרסה ישנה").

1. דחוף קבצי תוכן בלבד (app.js, index.html, tests, proxy…).
2. אמת: `APP_VERSION` ב־raw.githubusercontent.com **וגם** ב־GitHub Pages החי.
3. המתן ~60 שניות.
4. דחוף את `sw.js` בנפרד.
5. אמת חי: `APP_VERSION`, `CACHE_NAME` (חייבים להסתיים באותה גרסה — **מגביהים תמיד יחד**), וה־UI החדש.
6. אם לקוח כבר הורעל — bump גרסה נקי (v+1, בלי שינוי תוכן) מאלץ התקנה נקייה.

השרתון (Vercel) מתפרס אוטומטית מכל push לריפו — אין צעד ידני.

## 9. כללים נוספים

- **i18n**: כל מחרוזת גלויה חייבת להתקיים ב־`STRINGS.he` וב־`STRINGS.en`. הבדיקה אוכפת.
- **עיצוב**: עברית תחילה; טקסט ואייקונים **מעט גדולים מהסטנדרט**; היברידי — תחושת Material מוכרת לאנדרואיד עם טעם של Apple (Liquid Glass כבסיס craft); בהיר + כהה; **אסור לשנות `space-2`**; צבעים נועזים אך נעימים כמו אפליקציית Google טבעית; תצוגת הפקדות נפרדת עם תאריך מדויק לכל הפקדה.
- **יישור עיצובי (v151)**: היררכיית כפתורים — ראשי = ירוק מלא (`.btn`), משני = מתאר, הרסני משני = מתאר אדום (`#ibkrDisconnect`), הרסני סופי = אדום מלא (כפתורי האיפוס בלבד), פעולת שורה = `mini-btn` (ירוק לעריכה, `mini-btn danger` למחיקה) — בכל הטאבים, לא קישורים ולא ✕. ירוק בטקסט = רווח בלבד (משיכה באפור). כרטיס מניה: סימבול+תגית בשורה 1, שם בשורה 2 עם ellipsis. יישור טקסט רק לוגי (`start`/`end`, `justify-self`, `margin-inline-*`) — `layout.test` אוסר left/right. כלי צילום לפני/אחרי: סקריפט Playwright עם נתוני דמו (14 מסכים × בהיר/כהה) — להריץ לפני כל שינוי עיצובי. **v152**: עיגולי פינות סטנדרטיים = משתנים בלבד (`--pill` 999, `--radius-xs` 12, `--radius-sm` 16, `--radius` 22, `--radius-xl` 28; `tests/ui-v152.test.js` אוכף). לקח: "ניקיון" של גדלי טקסט (עיגול חצאי פיקסל) **כן** משנה את המראה — שורות נשברות מחדש; לבדוק בהשוואת פיקסלים עם `clock.setFixedTime` לפני שמכריזים "בלי שינוי נראה".
- **ניווט ורענון (v153)**: רענון חוזר לטאב האחרון (`pwa_lasttab_v1`) ולנקודת הגלילה שלו (`pwa_lastscroll_v1`, לכל טאב). **אסור מעבר כפוי לטאב אחר בהפעלה** (היה `if (!tdKey()) switchTab('settings')` — כל רענון נחת בהגדרות). השחזור: `restoreScrollTo` מנסה כל 120ms עד שהדף ארוך מספיק (עד 8 שניות), לא שומר מיקום בזמן השחזור (`scrollRestoring()`), נעצר במגע/גלילה של המשתמש; `history.scrollRestoration='manual'`. קפיצה מכוונת לראש העמוד — לקרוא `cancelScrollRestore()` לפני `scrollTo(0,0)`.
- **סינון מקור + סדר הפקדות (v155→v156)**: מ־v156 זה **כפתור "סינון" אחד** בכותרת (בקצה החיצוני של `.head-actions` — כך הבועה נפתחת לתוך המסך; בקצה הפנימי היא גלשה מימין) עם בועת בחירה (`.src-pop`, `menu-drop`, `closeSrcPops`) — לא שורת צ'יפים גלויה (בקשת המשתמש). מסונן = הכפתור ירוק; **התווית תמיד "סינון"** (v157, בקשת המשתמש — לא "IB"/"ידני"). האפשרויות: "הכל / ידני / IB" בהפקדות/עסקאות/מניות (`renderSrcFilter(tab)`, `getSrcFilter`, `srcPass`, `pwa_srcfilter_v1` לכל טאב) — המקור תמיד מ־`isIbkrDeposit`/`positionSource`, לא כלל חדש. הפקדות מוצגות מהחדשה לישנה (`depositsNewestFirst` שומר אינדקס מקורי — עריכה/מחיקה לפיו). שורות `.rows`: הטקסט הראשון מתכווץ ונשבר, השאר `flex:none` — לקח: `min-width:0` על כל הילדים נתן לסכום להתכווץ ולגלוש מהמסך.
- **כפתורי ההדר (v154)**: לכל כפתור עגול שני "פנים" (`btnFacesHTML(main, alt)`, `.face-main`/`.face-alt`). המבורגר פתוח = `.menu-open` על `.appbar-actions`: גלובוס→הגדרות, $→בהיר/כהה (אייקון הערכה שאליה עוברים), ☰→✕. פתיחה/סגירה רק דרך `setMainMenuOpen`/`mainMenuOpen` (מעדכן גם תוויות נגישות). אותו סגנון כפתור בכל המצבים — רק האייקון מתחלף (בקשת המשתמש). בתוך התפריט נשאר רק "מזומן בתיק" — לא להחזיר לשם כפתורי הגדרות/ערכה. כתיבה לכפתור המטבע: רק ל־`.face-main` (לא `textContent` של הכפתור — מוחק את הפנים השניות).
- **v132**: `.fx-pill` (פיל שער דולר־שקל) השתמש במשתני CSS לא קיימים (`--card`, `--border`, `--subtle`, `--text`) עם נפילה ל־fallback קבוע — נשאר לבן במצב כהה. תוקן לטוקנים האמיתיים (`--surface`, `--card-border`, `--on-surface-var`, `--on-surface`, `--shadow`). לקח: `var(--x, fallback)` עם שם משתנה שגוי נכשל בשקט — שווה לחפש `var(--[a-z-]+, ` בלי לוודא הגדרה.
- **יבוא CSV הוסר לגמרי (v128, בקשת המשתמש 24/09/2026)** — מקור הנתונים היחיד של מצב IBKR הוא סנכרון Flex. לא להחזיר. ("CSV" שנשאר בקוד = היסטוריית מחירים מ־Stooq, `parseHistoryCSV` — לא קשור.)
- **כרטיס IBKR בהגדרות (v128)**: גלויים רק סטטוס + "סנכרן וייבא" + "ניתוק"; טווח המשיכה, הגדרות החיבור וההדרכה ב־`<details>` מקופלים (`ibkrRangeDetails`, `ibkrConnDetails` — נפתח לבד כשאין token/Query ID, `ibkrFlexHow`). העדפת המשתמש: מעט טקסט גלוי, הרחבה בחיצים.
- **תרשים עוגה (חלוקת תיק מניות, v133)**: קנבס מרובע (`aspect-ratio:1/1`) שגדל עם רוחב הכרטיס עד ~460px, במקום 340×220 קבוע. צבע כל חברה = **גוון פסטלי** (לא רווי) שנגזר מ־hue צבע המותג האמיתי שלה — `PIE_BRAND_COLORS` (app.js, מקורות ציבוריים brandcolors.net/brandpalettes.com) → `pastelizeBrand()` (מגביל רוויה ל־≤0.55, מעלה בהירות ל־0.68–0.8, שומר את ה־hue). חברה לא ממותגת מקבלת גוון מ־`PIE_FALLBACK_PALETTE` (בהשראת גרף העוגה הפסטלי שהמשתמש שלח כדוגמה). כמה מהמותגים בתיק (META/INTU/MBLY/UNH) הם "כחולים" רשמיים שונים שהתכנסו לאותו גוון פסטלי בקירוב — `pieDedupeColors()` מזיזה hue (חיפוש בין 24 גוונים מרווחים ב־15°, לא "צעד־אחר־צעד" שנתקע באזור צפוף) לכל חברה שקרובה מדי לחברה שכבר "ננעלה", **לפני** הסידור לטבעת — כדי שאין שתי חברות בצבע כמעט זהה גם מחוץ לשכנות. `pieArrangeSlices()` (קיים מקודם) מוודאת בנוסף שאין שתי **פרוסות שכנות** בטבעת (כולל התפר המעגלי) קרובות מדי. לוגו כל חברה (מקור `financialmodelingprep.com/image-stock/`, כמו בטאב המניות) מצויר בתוך הפרוסה שלה על תג לבן בגודל פרופורציוני; פרוסה צרה מדי מדלגת על לוגו. לוגו קטן ליד עיגול הצבע בכל שורת מקרא (RTL: הלוגו לפני ה־dot ב־DOM = ימני יותר). `tests/pf-v133.test.js` (32 בדיקות) — כולל בדיקת drawPie קצה־לקצה עם קנבס מדומה (Proxy). לקח: 8 צבעי מותג פסטליים על מעגל 360° זה כמעט מלא — אחרי `pieDedupeColors` נשארו זוגות בורדרליין (מרחק ~0.13–0.14 מתוך סף 0.15) שלא הצליחו להתרחק לגמרי; קביל ויזואלית, אבל אם עוד חברות ממותגות בכחול יתווספו לתיק — שווה לשקול הרחבת הסף/הרוויה.

## 10. המשתמש וכללי התנהגות

- המשתמש, דובר עברית, סגנון תמציתי וישיר ("תסדר גם את זה"). מטפל במשימות מקצה לקצה — לא מדריך אותו צעד־אחר־צעד.
- **כלל הברזל**: לאמת שהעדכון באמת עובד לפני שדוחפים (מ־2026-09-22). **לעולם לא להכריז "תוקן / מהיר / חי" עד שהטלפון שלו מאשר** (לקח v48: הכרזתי "נטען מהר" כשהגרף עוד נטען לאט אצלו). הטלפון שלו הוא ground truth היחיד ל־"חי".
- תשובות בעברית כשהוא כותב עברית; תמציתי ומעשי; בלי "שאלה מצוינת!".
- לא להציע מחדש מה שנדחה: פרוקסי overnight/Alpaca (נדחה 2026-09-23), קריאת צ'אטי וואטסאפ אחרים (בלתי אפשרי), APK (לא רוצה), טיוטות פרסונליזציה.
- תדריך בוקר יומי ב־07:00 Asia/Jerusalem (cron בצד Muse) — מסמך הפורמט טרם הגיע מהמשתמש; לא לנדנד.

## 11. פרוטוקול שני מנהלים (Muse + Claude)

- **הריפו הוא מקור האמת היחיד.** לפני כל עבודה: למשוך/לקלון את הגרסה העדכנית.
- כותב אחד בכל פעם לאותו קובץ. מכריזים כוונה ביומן העבודה.
- מספרי גרסה: תמיד מול הגרסה החיה העדכנית (בודקים ב־raw GitHub).
- שני המנהלים מעדכנים את `CLAUDE.md` ואת `יומן-עבודה.md` כשלומדים משהו עמיד.
- Muse דוחף דרך ה־connector שלו; Claude דרך החיבור שלו ל־GitHub — לשניהם הרשאות כתיבה מלאות.
- כשנגמרים טוקנים בצד אחד — עוברים לצד השני; הוא קורא קובץ זה + זנב היומן וממשיך מאותה נקודה.

## 12. עבודה מהטלפון (Claude Code / Claude app) — התאמות

- **GitHub Actions מריץ את כל הבדיקות בענן** בכל push/PR (`.github/workflows/test.yml`): גם בלי להריץ כלום מקומית רואים ירוק/אדום מהטלפון. חובה ירוק לפני פריסה.
- **Claude Code ב־Termux (אנדרואיד)**: `pkg install nodejs git`, `npm i -g @anthropic-ai/claude-code`, התחברות, `git clone` הריפו. קובץ זה (CLAUDE.md) נטען אוטומטית. התאמות: `termux-wake-lock` + ביטול אופטימיזציית סוללה ל־Termux (אנדרואיד הורג תהליכי רקע), push דרך `gh` או git עם PAT, אימות חי עם `curl` כמו בסעיף 8. `sleep 60` עובד רגיל לפרוטוקול הדו־שלבי.
- **Claude app + חיבור GitHub**: יכול לקרוא קבצים ולהציע שינויים; דחיפה ישירה ל־main תלויה בהרשאות החיבור — אם לא, עובדים דרך PR שהמשתמש ממזג מהטלפון. את פרוטוקול הפריסה הדו־שלבי (סעיף 8) מבצעים בצ'אט בצעדים מפורשים: "דחוף תוכן, אמת, ורק אחרי אישור שלי דחוף sw.js".
- בכל מקרה: לעולם לא להריץ בדיקות מול הטוקן האמיתי; mocks בלבד.

## 13. צ'קליסט פתיחת סשן

1. [ ] קראתי `CLAUDE.md` (קובץ זה).
2. [ ] קראתי את זנב `יומן-עבודה.md`.
3. [ ] בדקתי גרסה חיה מול מקומית (`APP_VERSION`, `CACHE_NAME`).
4. [ ] משכתי את העדכני מהריפו לפני עריכה.

## 14. פריטים פתוחים (2026-09-24)

- תצוגת עסקאות באפליקציה — כל קנייה/מכירה עם מחיר ועמלה ("כמו Yahoo Finance").
- סימון `kind` לקרנות/הפקדות של קרן השתלמות (מצב עריכה בהגדרות) — לפיצול פנסיוני נכון.
- פריטי המבורגר נוספים — נשאל ב־2026-09-23, טרם נענה.
- ניקוי הערות/קאשים מיושנים, אזהרות TWR רשמי, ניקוי היסטוריה ציבורית.
- v124 ו־v125 אושרו בטלפון (24/09/2026): 3 שנים מ־1/1/2023; גרף הביצועים עם NAV יומי — "כל הנתונים נראים מעולים". לקח: אחרי שינוי בפענוח השרתון צריך ניתוק + סנכרון מחדש — נתונים שמורים לא מתעדכנים לבד.
