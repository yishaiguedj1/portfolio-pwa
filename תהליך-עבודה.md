# תהליך העבודה — מבקשה ועד "עובד בטלפון"

> המסמך הזה מתאר איך עובדים על האפליקציה בפועל, צעד אחר צעד.
> הכללים עצמם (פרטיות, IBKR, פריסה) נמצאים ב־`CLAUDE.md`, וכאן רואים איך הם מתחברים לרצף עבודה אחד.
> דוגמה חיה לאורך המסמך: v121, תיקון המשיכה האיטית מ־IBKR (24/09/2026).

## מי עושה מה

| תפקיד | מי | מה עושה |
|---|---|---|
| בעל המוצר | ישי | מבקש, שולח צילום מסך מהטלפון ומאשר שזה עובד. **הטלפון שלו הוא האישור היחיד ל"חי".** |
| מנהל | Claude | מאבחן, מתקן, בודק, פורס ומתעד. |
| מנהל | Muse | אותו תפקיד, דרך connector משלו. |

הריפו הוא מקור האמת היחיד. מי שנכנס (Claude או Muse) קורא את `CLAUDE.md` ואת זנב היומן, וממשיך מהנקודה שבה השני עצר.

## שלב 0 — פתיחת סשן

1. קוראים את `CLAUDE.md` ואת זנב `יומן-עבודה.md`.
2. מושכים את העדכני: `git pull origin main`, כי ייתכן ש־Muse דחף בינתיים.
3. משווים גרסה מקומית לגרסה החיה (`APP_VERSION` ב־`app.js` ו־`CACHE_NAME` ב־`sw.js`), ב־raw.githubusercontent.com וב־GitHub Pages.
4. בודקים הרשאות כתיבה, אם זה סשן חדש. עובדים ישירות על `main`, כפי שישי אישר.

## שלב 1 — הבקשה

ישי כותב בקצרה מה מפריע, בדרך כלל עם צילום מסך מהטלפון.
> דוגמה: "המשיכה מ־IB לוקחת מלא זמן… שעדיין נוכל לקבל 10 שנים, אבל שלא ייקח כל כך הרבה זמן."

כללים:
- מטפלים מקצה לקצה, לא מדריכים את ישי צעד־אחר־צעד.
- לא חוזרים על פרטים רגישים מצילום המסך (Query ID, טוקן, מספר חשבון).
- לא מציעים שוב דברים שנדחו (ראו `CLAUDE.md` סעיף 10).

## שלב 2 — אבחון (שורש, לא סימפטום)

1. מאתרים את הקוד הרלוונטי בחיפוש ממוקד. לא קוראים את כל `app.js`, כי הוא ענק.
2. מחשבים איפה הזמן או הבאג באמת נמצא.
3. מנסחים את השורש במשפט אחד.

> דוגמה (v121): משיכה של 5 שנים היא 6 חלקים של 365 יום. בין כל חלק הייתה הפוגה קבועה של 60 שניות, והשאילתה הראשונה "הדוח מוכן?" נשלחה מיד. היא כמעט תמיד חזרה "עדיין מייצר" ושרפה בקשה ועוד 12 שניות. 10 שנים לקחו כ־13 דקות, ורובן היו המתנה ריקה.

## שלב 3 — תיקון

- תיקון מינימלי שפותר את השורש, בלי להרחיב את ההיקף.
- שומרים על כל ההגנות הקיימות. ב־v121 נשארו: בלי ניסיון חוזר על 1018, עצירה מיידית על 1025, ומפסק מעגל.
- כל מחרוזת גלויה חדשה נכתבת בעברית **ובאנגלית** (`STRINGS.he` / `STRINGS.en`).
- מעלים את `APP_VERSION` ב־`app.js` (למשל v120 ← v121). את `sw.js` **לא נוגעים עדיין**.

> דוגמה: במקום הפוגה קבועה, מגביל קצב מתגלגל (`IBKR_LIMITER`): עד 8 בקשות בכל 60 שניות (התקרה של IBKR היא 10), לפחות 1.5 שניות בין בקשות, וממתינים רק כשהמכסה באמת נגמרה.

## שלב 4 — בדיקות

1. `node --check app.js`, תמיד.
2. הבדיקות הרלוונטיות, למשל `node tests/ibkr-app.test.js`.
3. מוסיפים בדיקות לשינוי עצמו. שינוי בקצב נבדק ב**שעון מדומה**, בלי לישון באמת ובלי לפנות ל־IBKR.
4. כל קבצי הבדיקה, כמו הלולאה ב־`.github/workflows/test.yml`, ובנוסף `node ibkr-proxy/tests/run.js`.

כללים:
- **לעולם לא** בודקים מול הטוקן האמיתי. mocks בלבד.
- בין דחיפת התוכן לדחיפת `sw.js`, בדיקות התאימות `CACHE_NAME`↔`APP_VERSION` אדומות. זה צפוי. מוודאים מקומית שהכל ירוק כש־`sw.js` מעודכן, ואז מחזירים אותו לגרסה הקודמת עד שלב 6.

> דוגמה: סימולציה של 10 שנים יצאה 33 בקשות וכ־4 דקות, ובאף דקה לא יצאו יותר מ־8 בקשות. ibkr-app עלה ל־181 אסרטים.

## שלב 5 — תיעוד לפני הפריסה

- מוסיפים ל־`יומן-עבודה.md` רשומה בעברית: התקלה, השורש, התיקון, הבדיקות.
- אם נלמד לקח עמיד, מעדכנים את `CLAUDE.md` (קבועים, מוסכמות בדיקה, פריטים פתוחים).

## שלב 6 — פריסה דו־שלבית (הכי קריטי)

**לעולם לא דוחפים את `sw.js` יחד עם קבצי התוכן.** אם טלפון מתקין SW חדש כשה־CDN עוד מגיש `app.js` ישן, הקאש מורעל והאפליקציה נתקעת על גרסה ישנה.

| # | פעולה | איך מוודאים |
|---|---|---|
| 1 | commit ודחיפה של **תוכן בלבד** (app.js, tests, CLAUDE.md, יומן…) | `git push origin main` |
| 2 | אימות שהתוכן חי | `APP_VERSION` החדש מופיע ב־raw **וגם** ב־GitHub Pages. בודקים שוב ושוב עד שמופיע, בדרך כלל תוך פחות מדקה. |
| 3 | המתנה | לפחות 60 שניות מהרגע שהתוכן חי |
| 4 | מעלים את `CACHE_NAME` ב־`sw.js` לאותה גרסה, ודוחפים **רק אותו** | commit נפרד: "vNNN: service worker" |
| 5 | אימות סופי | באתר החי `APP_VERSION` ו־`CACHE_NAME` מסתיימים באותה גרסה, והקוד החדש קיים בקובץ (למשל `ibkrMakeLimiter`) |
| 6 | CI | ריצת `tests` ב־GitHub Actions על ה־commit האחרון ירוקה |

אם טלפון כבר הורעל: מעלים גרסה ב־1 בלי שום שינוי תוכן, וזה מכריח התקנה נקייה.
השרתון ב־Vercel מתפרס לבד בכל push, אין צעד ידני.

## שלב 7 — דיווח לישי

- תמציתי ובעברית: מה היה השורש, מה שונה, מה נבדק, מה נפרס.
- **לא כותבים "תוקן", "מהיר" או "חי" לפני שהטלפון שלו מאשר.** מספרים מסימולציה מציגים כהערכה.
- מבקשים ממנו לבדוק בטלפון ולדווח (למשל: "תנסה משיכה ותגיד כמה זמן זה לקח").

## שלב 8 — סגירה

1. ישי מאשר מהטלפון.
2. רושמים ביומן "vNNN אושר בטלפון ✅", ומעדכנים את "פריטים פתוחים" ב־`CLAUDE.md`.
3. אם הוא לא מאשר, חוזרים לשלב 2 עם המידע החדש. לא מנחשים.

## צ'קליסט מקוצר

- [ ] קראתי CLAUDE.md וזנב היומן, ומשכתי את העדכני
- [ ] אבחנתי שורש, במשפט אחד
- [ ] תיקון מינימלי, i18n בשתי השפות, `APP_VERSION` עלה
- [ ] `node --check` ובדיקות ירוקות (mocks בלבד)
- [ ] יומן ו־CLAUDE.md מעודכנים
- [ ] דחפתי תוכן ← אימתתי raw ו־Pages ← חיכיתי 60 שניות ← דחפתי `sw.js` לבד ← אימתתי חי ו־CI ירוק
- [ ] דיווחתי בלי להכריז "תוקן", וממתין לאישור טלפון
